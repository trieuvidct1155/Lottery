@model IEnumerable<BANVESO.Models.LOAIVESO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiVS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiVS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiVS }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiVS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiVS })
        </td>
    </tr>
}

</table>
